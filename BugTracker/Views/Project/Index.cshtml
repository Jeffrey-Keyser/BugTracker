
@model IEnumerable<BugTracker.Models.UserProject>;
@{
    ViewData["Title"] = "Project Details";
}

<div class="container row p-0 m-0">
    <div class="col-8">
        <h2 class="text-info">Project List</h2>
    </div>
    <div>
        @Html.ActionLink("Create New Project", "Create")
    </div>
</div>
<br />

<ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item">
        <a class="nav-link active" id="my-projects-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home"
           aria-selected="true">My Projects</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="assigned-projects-tab" data-toggle="tab" href="#assigned" role="tab" aria-controls="assigned"
           aria-selected="false">Assigned Projects</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="completed-projects-tab" data-toggle="tab" href="#completed" role="tab" aria-controls="completed"
           aria-selected="false">Completed Projects</a>
    </li>
</ul>
<div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="my-projects-tab">
        <div class="col-12 border p-3 mt-3">
            <form method="post">
                @if (Model.Count() > 0)
                {
                    <table id="proj_table" class="display" style="width:100%">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Project Name</th>
                                <th>Author</th>
                                <th>Programming Language</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                if (item.Project.userId == ViewBag.userId && !item.Project.completed)
                                {
                                    <tr>
                                        <td>
                                        </td>
                                        <td>
                                            @Html.ActionLink(item.Project.ProjectName, "Project", "Project", new { id = item.projectId })
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Project.Author)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Project.projectLanguage)
                                            <div id="parent_div_1">
                                                <div class="child_div_2">


                                                    @using (Html.BeginForm("Delete", "Project", new { id = item.projectId }))
                                                    {
                                                        @Html.AntiForgeryToken()

                                                        <a style="color:whitesmoke" class="btn btn-danger delete"
                                                           data-id="@item.projectId"
                                                           data-controller="Project"
                                                           data-action="Delete"
                                                           data-body-message="Are you sure you wish to delete?"
                                                           data-redirect-url="/Project/Index"> Delete </a>

                                                    }
                                                </div>
                                                <div class="child_div_2">
                                                    @Html.ActionLink("Edit", "Edit", new { id = item.projectId })
                                                </div>
                                            </div>
                                        </td>

                                    </tr>
                                }
                            }

                        </tbody>
                        <tfoot>
                        </tfoot>
                    </table>
                }
                else
                {
                    <p> No Projects to Display! </p>
                }
            </form>
        </div>
    </div>
    <div class="tab-pane fade" id="assigned" role="tabpanel" aria-labelledby="assigned-projects-tab">

        <div class="col-12 border p-3 mt-3">
            <form method="post">
                @if (Model.Count() > 0)
                {
                    <table id="proj_table2" class="display" style="width:100%">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Project Name</th>
                                <th>Author</th>
                                <th>Programming Language</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                if (item.Id == ViewBag.userId && item.Project.userId != ViewBag.userId && !item.Project.completed)
                                {
                                    <tr>
                                        <td>
                                        </td>
                                        <td>
                                            @Html.ActionLink(item.Project.ProjectName, "Project", "Project", new { id = item.projectId })
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Project.Author)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Project.projectLanguage)
                                            <div id="parent_div_1">
                                                <div class="child_div_2">


                                                    @using (Html.BeginForm("Delete", "Project", new { id = item.projectId }))
                                                    {
                                                        @Html.AntiForgeryToken()

                                                        <a style="color:whitesmoke" class="btn btn-danger delete"
                                                           data-id="@item.projectId"
                                                           data-controller="Project"
                                                           data-action="Delete"
                                                           data-body-message="Are you sure you wish to delete?"
                                                           data-redirect-url="/Project/Index"> Delete </a>

                                                    }
                                                </div>
                                                <div class="child_div_2">
                                                    @Html.ActionLink("Edit", "Edit", new { id = item.projectId })
                                                </div>
                                            </div>
                                        </td>

                                    </tr>
                                }
                            }

                        </tbody>
                        <tfoot>
                        </tfoot>
                    </table>
                }
                else
                {
                    <p> No Projects to Display! </p>
                }
            </form>
        </div>

    </div>
    <div class="tab-pane fade" id="completed" role="tabpanel" aria-labelledby="completed-projects-tab">

        <div class="col-12 border p-3 mt-3">
            <form method="post">
                @if (Model.Count() > 0)
                {
                    <table id="proj_table1" class="display" style="width:100%">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Project Name</th>
                                <th>Author</th>
                                <th>Programming Language</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                if (item.Project.userId == ViewBag.userId && item.Project.completed)
                                {
                                    <tr>
                                        <td>
                                        </td>
                                        <td>
                                            @Html.ActionLink(item.Project.ProjectName, "Project", "Project", new { id = item.projectId })
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Project.Author)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Project.projectLanguage)
                                            <div id="parent_div_1">
                                                <div class="child_div_2">


                                                    @using (Html.BeginForm("Delete", "Project", new { id = item.projectId }))
                                                    {
                                                        @Html.AntiForgeryToken()

                                                        <a style="color:whitesmoke" class="btn btn-danger delete"
                                                           data-id="@item.projectId"
                                                           data-controller="Project"
                                                           data-action="Delete"
                                                           data-body-message="Are you sure you wish to delete?"
                                                           data-redirect-url="/Project/Index"> Delete </a>

                                                    }
                                                </div>
                                                <div class="child_div_2">
                                                    @Html.ActionLink("Edit", "Edit", new { id = item.projectId })
                                                </div>
                                            </div>
                                        </td>

                                    </tr>
                                }
                            }

                        </tbody>
                        <tfoot>
                        </tfoot>
                    </table>
                }
                else
                {
                    <p> No Projects to Display! </p>
                }
            </form>
        </div>


    </div>
</div>



@section Scripts{
    <script src="~/js/DataTables/DataTable1.js"></script>
    <script src="~/js/DataTables/DataTable2.js"></script>
    <script src="~/js/DataTables/DataTable3.js"></script>
    <script src="~/js/delete.js"></script>
}
